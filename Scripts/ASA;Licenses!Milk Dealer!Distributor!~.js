/*totalVolume();
useAppSpecificGroupName = true;
totalAmt = false;
if (typeof(PURCHASINGMILKFROM) == "object") {
	for(x in PURCHASINGMILKFROM) if(PURCHASINGMILKFROM[x]["State"] != "NY") totalAmt = true;
	}

if (if(totalAmt = true);) {
	AInfo['Hold Import Permit'].hidden= true;
	}

useAppSpecificGroupName = false;
var total = parseFloat(AInfo['Monthly sale (Pounds)']) + parseFloat(AInfo['Total Volume Purchased']);
editAppSpecific("Total Volume",total.toString());
*/